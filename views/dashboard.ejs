<!DOCTYPE html>
<html>
<head>
  <title>Crew Dashboard</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="container mt-5">
  <h1>Welcome, <%= pseudonym %>!</h1>
  <p><strong>Total Points:</strong> <%= totalPoints %></p>
  <p>
  <strong>Achievements:</strong>
  <span class="<%= unlocked.length === achievements.length ? 'text-success fw-bold' : '' %>">
    <%= unlocked.length %> / <%= achievements.length %>
  </span>
</p>


  <div class="row">
    <% achievements.forEach(a => { %>
      <div class="col-md-4 mb-4">
        <div class="card">
          <img src="<%= a.image %>" class="card-img-top <%= unlocked.includes(a.id) ? '' : 'locked' %>" alt="Achievement">
          <div class="card-body">
            <h5 class="card-title"><%= a.title %></h5>
            <p class="card-text"><%= a.description %></p>
            <span class="badge bg-success"><%= a.points %> pts</span>
          </div>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>